__pycache__/
*.pyc
/.venv/
venv/
db.sqlite3
.env
.env.local

# Artefactos de coverage.py / pytest-cov (monolito y microservicios)
.coverage
.coverage.*
.pytest_cache/
htmlcov/

# Artefactos generados / contenido de usuarios, no se versionan
/staticfiles/
/media/

# Backups locales generados por scripts/backup.sh - nunca se versionan
/backups/

# Herramientas locales de Claude, no se versionan
CLAUDE.md
graphify-out/
.claude/

# Stack Docker local (nginx sin HTTPS), solo para pruebas en esta maquina
docker-compose.override.yml
nginx.local.conf

# Hoja de ruta de trabajo personal, no se versiona
ROADMAP.local.md